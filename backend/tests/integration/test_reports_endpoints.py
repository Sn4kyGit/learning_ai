"""
Integration tests for report generation endpoints.

This module serves as the main entry point for report tests.
The actual test classes have been moved to specialized modules
for better organization and maintainability.

To add new report test classes:
1. Create a new file in backend/tests/integration/reports/
2. Import the test class here for backward compatibility
3. Keep each test file under 500 lines
"""

# Import test classes from specialized modules
# Note: Test classes can be extracted from the original large file
# and placed in focused modules like:
# - test_configuration.py (TestReportConfiguration)
# - test_weekly_reports.py (TestWeeklyReports)  
# - test_custom_reports.py (TestCustomReports)
# - test_email_reports.py (TestReportEmail)
# - test_access_control.py (TestReportAccessControl)
# - test_performance.py (TestReportPerformance)

# For now, maintaining minimal structure to avoid breaking existing tests
# while demonstrating the reorganization pattern

__all__ = []